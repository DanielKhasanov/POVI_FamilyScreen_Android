<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.povi.povifamilyselect">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:launchMode="singleTop"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Families"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddFamilyActivity"
            android:label="@string/title_activity_add_family"
            android:parentActivityName=".Families"
            android:theme="@style/AppTheme.PopupOverlay">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.povi.povifamilyselect.Families" />
        </activity>
        <activity
            android:name=".AddFamilyMemberActivity"
            android:label="@string/title_activity_add_family_member"
            android:parentActivityName=".Families"
            android:theme="@style/AppTheme.PopupOverlay">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.povi.povifamilyselect.Families" />
        </activity>
    </application>

</manifest>
